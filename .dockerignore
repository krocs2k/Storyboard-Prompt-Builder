# Dependencies (will be installed fresh in Docker)
node_modules

# Build outputs
.next
.build
out
dist
build

# Development / IDE
.git
.gitignore
.vscode
.idea
.env.local
.env.development
.env.test
*.log
coverage
.nyc_output

# OS files
.DS_Store
Thumbs.db

# Abacus.AI platform files (not needed in Docker)
.abacus.donotdelete
.deploy

# Docs (keep DOCKER-DEPLOY.md readable but skip others)
# NOTE: Do NOT ignore yarn.lock.bak — it's the resolved lock file for Docker
